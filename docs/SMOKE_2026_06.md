# Post-deploy smoke — June 2026

**Date:** 2026-06-03  
**Target:** Production — [https://www.zedapply.com](https://www.zedapply.com) / API [https://api.zedapply.com/api/v1](https://api.zedapply.com/api/v1)  
**Repo ref:** `master` @ `160aac4` (agent workspace)  
**Method:** Read-only Supabase SQL, public `curl`, Vitest/pytest on `master` (no `ADMIN_API_KEY` or user JWT in Cloud Agent env).

**Legend:** **PASS** | **FAIL** | **BLOCKED** (needs admin/super session or API key) | **CODE** (tests/code on `master`; prod not fully deployed)

---

## Preconditions

| Check | Result | Evidence |
| --- | --- | --- |
| Merge train through PR #253 deployed | **BLOCKED** | Not verifiable from this agent (no GitHub deploy metadata). Proceed assuming maintainer confirmed. |
| Migrations `100+` applied on prod DB | **FAIL** | `supabase_migrations.schema_migrations`: latest named migration **`099_admin_stats_job_review_counts`** (`20260603081919`). No ledger rows for `100_in_app_notifications`, `101_admin_broadcast_notifications`, `102_admin_stats_jobs_active_public`, `103_zambia_skill_aliases_fix`. Table `notifications` **exists** (likely partial/manual apply). |
| Backend force-recreate on OCI | **FAIL** | `GET /api/v1/notifications` → **404 Not Found** (route present on `master` in `main.py` but absent on live API). Suggests backend image/env not refreshed to current `master`. |
| API health | **PASS** | `GET /api/v1/health` → `status: healthy`, `supabase: true`, `waha: true`, `vapid_configured: true`. |

---

## Checklist (14 items)

| # | Area | Result | Notes / evidence |
| --- | --- | --- | --- |
| 1 | Matches: Super delivered counter not `0 · Unlimited` when deliveries exist | **CODE** + **BLOCKED** | **DB:** Super user `5d6c1f43-…` has **33** `matches.credited_at` this month, **0** active (all dismissed). **Tests:** `matches/__tests__/page.test.tsx` + `lib/__tests__/matchQuota.test.ts` assert `resolveMatchQuotaDisplay` shows `N · Unlimited` (not `0 · Unlimited`) when `remaining_quota` implies usage. **Live UI:** needs Super session on `/matches`. |
| 2 | Hide match: no “Not Found” toast | **CODE** + **BLOCKED** | Backend idempotent dismiss (`status == dismissed` → 200). **Tests:** `test_match_dismiss.py` (28 passed), matches page dismiss flow test. **Live:** confirm hide on an active match; 404 still surfaces API `detail` as toast text if mis-invoked. |
| 3 | CV builder: skills in preview; print PDF page 1 header+summary contiguous | **CODE** | **Tests:** `skillsDisplay.test.ts`, `printTailoredCv.test.ts`, `AtsLivePreview.test.tsx` (30 passed). `print.css` sets `break-after: avoid` on header + summary. **Live print:** **BLOCKED** (browser). |
| 4 | Home dark §02 readable; hero cards faster | **PASS** (deployed CSS) + **CODE** | **Prod CSS:** `animate-float` / `float-delayed` / `float-delay-2` at **4.69s** cycle, delays **0.34s** / **0.67s** (`ANIMATION_SPEED_FACTOR = 0.67`). **§02:** `ScoreMathSection` uses `text-foreground` / `text-muted-foreground` (WCAG-oriented). **Tests:** `ScoreMathSection.test.tsx`, `hero-animation.test.ts`. **Live dark toggle:** **MANUAL**. |
| 5 | Why this match: matched + missing skills | **CODE** + **BLOCKED** | `MatchExplanationModal` renders `matched_skills` / `missing_skills`; modal tests pass. **Live:** open “Why this match” on a scored card. |
| 6 | Profile checklist modal saves; ring updates | **CODE** + **BLOCKED** | `ProfileCompletenessChecklist.test.tsx` (7 passed). **Live:** profile ring + modal save. |
| 7 | Settings danger zone phone confirm; notifications weekly/muted | **CODE** + **BLOCKED** | `DangerSection.test.tsx` (exact phone confirm), `NotificationsSection.test.tsx` (weekly + mute). **Live:** `/settings/danger`, `/settings/notifications`. |
| 8 | Navbar notifications badge + mark read | **FAIL** (prod API) + **CODE** (UI gap on `master`) | **Prod:** `GET /api/v1/notifications` → **404**. **Repo:** `inAppNotifications` client exists; **no** navbar dropdown component wired on `master` (train #248/#257 not fully in UI). **BLOCKED** for E2E until backend recreate + inbox UI ship. |
| 9 | Admin overview: deactivated + need review counts | **CODE** + **BLOCKED** | **SQL (read-only):** `need_review=580`, `deactivated=648`, `active_public=354` (same filters as `admin_review_jobs.py` / `admin.py`). **Live:** `/admin/overview` with admin JWT. |
| 10 | Admin jobs: deactivate expired; search; modal edit | **BLOCKED** | **SQL:** 2 expired rows still in review queue (`closing_date < today`, `admin_reviewed_at` null). **Code:** `POST /admin/jobs/bulk-deactivate` with `expired_only` (no `dry_run` — do not run on prod without maintainer). Search/modal: **MANUAL** admin UI. |
| 11 | Admin send test notification to Super tier only | **BLOCKED** | Compose UI: `/admin/notifications` → audience **By subscription tier** → **Super Standard** (`NotificationsTab.tsx`). Separate smoke: `POST /admin/push/test` (admin JWT, VAPID). No automated send (destructive). |
| 12 | Dashboard quota aligns with matches | **CODE** + **BLOCKED** | Shared `resolveMatchQuotaDisplay` in `lib/matchQuota.ts` (used by matches + dashboard). **Tests:** `matchQuota.test.ts`. **Live:** compare `/dashboard` vs `/matches` quota card for same user. |
| 13 | Interview prep entry on match card (Super) | **CODE** + **BLOCKED** | `MatchPremiumActions` “Interview prep” when `onInterviewPrepClick` set; `MatchesPageClient` gates Super. **Tests:** `MatchCard.test.tsx`. **Live:** Super session on match with score. |
| 14 | Referral copy says “subscribe” (after 6B deploy) | **FAIL** | **Prod bundles:** no `paid tiers` string in sampled homepage chunks; referral card not in static HTML. **`master` copy:** ProfileReferralCard still says *“once you qualify **paid tiers**”* (not “subscribe”). Scope lock §6B = first **paid subscription** trigger — copy update not deployed. |

---

## Automated test summary (workspace `master`)

| Suite | Result |
| --- | --- |
| `pytest` `test_match_dismiss`, `test_match_quota`, `test_admin_jobs` | **28 passed** |
| Vitest: matches, matchQuota, profile checklist, settings, home, CV print, match card | **62+ passed** (see agent log) |

---

## Read-only prod probes (curl / SQL)

```bash
# Health
curl -sS https://api.zedapply.com/api/v1/health | python3 -m json.tool

# In-app inbox (expected 401/200 on deployed master; observed 404 on 2026-06-03)
curl -sS -o /dev/null -w '%{http_code}\n' https://api.zedapply.com/api/v1/notifications

# Admin stats without key (expect 403)
curl -sS https://api.zedapply.com/api/v1/admin/stats
```

**Supabase (project `chnesgmcuxyhwhzomdov`):** active subs — 1× `starter`, 1× `super_standard`; super user **33** credited matches this month.

---

## Recommended follow-ups (maintainer)

1. **Apply migrations `100`–`103`** in order per [NOTIFICATIONS_MIGRATIONS.md](./NOTIFICATIONS_MIGRATIONS.md); confirm ledger + `admin_stats()` from `102`.
2. **`docker compose build zedcv-backend && docker compose up -d --force-recreate zedcv-backend`** on OCI (not `restart`) so `/notifications` and merge-train routes match `master`.
3. Re-run **BLOCKED** rows with admin JWT + Super test account on `/matches`, `/admin/overview`, `/admin/jobs`, `/admin/notifications` (tier = Super Standard only).
4. **Item 14:** ship referral copy (“subscribe” / paid conversion) with payment-trigger PR (6B); re-smoke profile referral card.
5. **Item 10:** run bulk deactivate expired only after explicit approval (no dry_run on that endpoint — use staging or count via SQL first).

---

## Sign-off

| Role | Name | Date | Notes |
| --- | --- | --- | --- |
| Agent (automated) | Cursor Cloud | 2026-06-03 | Doc-only PR; prod UI smokes mostly **BLOCKED** without credentials. |
| Maintainer | | | Complete BLOCKED/MANUAL rows after backend recreate. |
