export const builderInputClass =
  "w-full h-10 px-3 rounded-md text-sm transition-colors focus:outline-none focus:ring-2 focus:ring-[var(--brand-500,#0E5C3A)] focus:ring-offset-0";

export const builderFieldStyle = {
  border: "1px solid var(--line-2)",
  background: "var(--surface)",
  color: "var(--ink)",
} as const;

export const builderLabelClass = "text-xs font-medium block mb-1.5";

export const builderCardStyle = {
  background: "var(--surface)",
  border: "1px solid var(--line-2)",
} as const;
